# Core source files for Shadow Worker
# Note: Main target is defined in root CMakeLists.txt

# This file is kept for organization purposes but actual target
# definition is in the root CMakeLists.txt

# Source files are collected via GLOB_RECURSE in root CMakeLists.txt
